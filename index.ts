import 'react-native-gesture-handler';

import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent initializes the development client and signed native builds.
registerRootComponent(App);
